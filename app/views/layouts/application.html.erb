<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "SnowFlow" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href='/'>SnowFlow</a>
          <div class="container-fluid nav-collapse">
            <div class="span5">
              <div class="btn-group">
                <button class="btn dropdown-toggle" data-toggle="dropdown">
                  <span class="caret"></span>
                  Home
                </button>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Home', '/'  %></li>
                  <li><%= link_to 'Contacts', '/contacts'  %></li>
                  <li><%= link_to 'About', '/about'  %></li>
                </ul>
              </div>
              <div class="btn-group">
                <%= link_to 'Catalogue' , '/catalogue' , class: 'btn' %>
              </div>
              <div class="btn-group">
                <button class="btn dropdown-toggle" data-toggle="dropdown">
                  <% if signed? %>
                      <span class="caret"></span>
                      Profile
                  <% else %>
                      <span class="caret"></span>
                      Sign in
                  <% end %>
                </button>
                <ul class="dropdown-menu">
                  <% if signed? %>
                      <li><%= link_to 'Profile', user_path(current_user)  %></li>
                      <li><%= link_to 'Sign out', '/signout' , method: :delete %></li>
                      <li><%= link_to 'Users list' , user_index_path %></li>
                  <% else %>
                      <li><%= link_to 'Sign in', '/signin'  %></li>
                      <li><%= link_to 'Sign up', '/signup'  %></li>
                  <% end %>
                </ul>

              </div>
              <% if signed? && current_user.admin? %>
                  <div class="btn-group">
                    <button class="btn dropdown-toggle" data-toggle="dropdown">
                      <span class="caret"></span>
                      Administration
                    </button>
                    <ul class="dropdown-menu">
                      <li><%= link_to 'Personal cabinet', admin_path(current_user)  %></li>
                      <li><%= link_to 'Orders list', '/orders' %></li>
                      <li><%= link_to 'Create new item', new_item_path  %></li>
                    </ul>

                  </div>
              <% end %>
            </div>
            <div class="span1 offset3">
              <% if signed? %>
                  <div class="btn-group">
                    <%= link_to "In your cart #{current_cart.get_total_positions}. It's include #{current_cart.get_total_items}, costs: #{current_cart.get_total_price}" , edit_cart_path(current_cart) , class: 'btn' %>
                  </div>
              <% end %>
            </div>

          </div><!--/.nav-collapse -->

        </div>
      </div>

    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span2">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">Our goods</li>
              <li><%= link_to 'All', '/catalogue' %></li>
              <% @categories.each do |elem| %>
                  <li><%= link_to elem.name, "/catalogue?page=1&category=#{elem.name}" %></li>
              <% end %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span10">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2014</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
